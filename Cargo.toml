[workspace]
members = [
    "common",
    "placer",
    "router",
    "cli"
]
resolver = "2"

[workspace.package]
description = "PARE — Placement And Routing Engine: a digital IC physical design tool written in Rust"

[profile.release]
debug = true
