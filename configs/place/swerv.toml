[input]
lef_files = ["inputs/swerv/Nangate45.lef"]
def_file = "inputs/swerv/swerv.def"
output_def = "output/swerv/placed.def"

[global_placement]
placer_max_iterations = 2000
convergence_threshold = 1e-4
electro_force_multiplier = 1000.0

[legalization]
algorithm = "abacus"
