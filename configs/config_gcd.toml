[global_placement]
target_density = 0.2
bin_dimension = 32
placer_max_iterations = 1000
initial_learning_rate = 0.005
convergence_threshold = 2e-4
wa_gamma = 400.0
electro_force_multiplier = 100.0

[legalization]
algorithm = "abacus"

[global_routing]
gcell_size = 10
capacity = 15
max_iterations = 100
history_increment = 1.0
initial_penalty = 1.0
penalty_multiplier = 1.5

[detailed_routing]
max_iterations = 200
history_increment = 0.5
initial_penalty = 0.5
penalty_multiplier = 1.3
capacity = 1
astar_window_margin_base = 10
astar_window_margin_max = 50
ripup_radius = 2
gcell_size = 2.0

[input]
lef_files = ["inputs/benchmarks/gcd.lef"]
def_file = "inputs/benchmarks/gcd.def"
output_def = "output/gcd_placed.def"
